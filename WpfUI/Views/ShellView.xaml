<Window x:Class="WpfUI.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfUI.Views"
        mc:Ignorable="d"
        Title="Menu Master" Height="287" Width="482">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="20"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>

        <Button x:Name="LoadDish" Grid.Row="1" Grid.Column="1" Margin="5">Load dish</Button>

        <ComboBox x:Name="DishesList" Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="1" Margin="5"
                  SelectedItem="{Binding Path=SelectedDish, Mode=TwoWay}"
                  DisplayMemberPath="Name"/>

        <ContentControl Grid.Row="1" Grid.Column="2" Grid.ColumnSpan="2" 
               Grid.RowSpan="3" x:Name="ActiveItem"/>

    </Grid>
</Window>
